<UserControl x:Class="TweetCat.App.Win.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="数据目录" FontWeight="Bold"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="{Binding DataRoot}" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Button Content="打开" Command="{Binding OpenCookiesCommand}"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="最近扩展心跳:" FontWeight="SemiBold" Margin="0,0,12,0"/>
            <TextBlock Text="{Binding LastPing}"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="扩展版本:" FontWeight="SemiBold" Margin="0,0,12,0"/>
            <TextBlock Text="{Binding ExtensionVersion}"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="Manifest 路径:" FontWeight="SemiBold" Margin="0,0,12,0"/>
            <TextBlock Text="{Binding ManifestPath}" Width="420" TextWrapping="Wrap" Margin="0,0,12,0"/>
            <Button Content="刷新" Command="{Binding RefreshStatusCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
