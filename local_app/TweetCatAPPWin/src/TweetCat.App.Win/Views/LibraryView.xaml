<UserControl x:Class="TweetCat.App.Win.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="刷新" Command="{Binding RefreshCommand}" HorizontalAlignment="Left"/>
        <ListView Grid.Row="1" ItemsSource="{Binding Entries}" Margin="0,16,0,0">
            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridViewColumn Header="标题" DisplayMemberBinding="{Binding Title}" Width="320"/>
                    <GridViewColumn Header="文件" DisplayMemberBinding="{Binding FilePath}" Width="320"/>
                    <GridViewColumn Header="来源" DisplayMemberBinding="{Binding SourceUrl}" Width="260"/>
                    <GridViewColumn Header="时间" DisplayMemberBinding="{Binding CreatedAt}" Width="160"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
