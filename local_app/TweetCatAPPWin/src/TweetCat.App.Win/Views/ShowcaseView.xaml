<UserControl x:Class="TweetCat.App.Win.Views.ShowcaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBox Width="420" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0"/>
            <Button Content="获取信息" Command="{Binding QueryCommand}" Margin="0,0,12,0"/>
            <Button Content="开始下载" Command="{Binding DownloadCommand}"/>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Border Height="220" Background="#222" CornerRadius="12" Margin="0,0,0,12">
                    <TextBlock Text="预览图" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
                </Border>
                <TextBlock Text="{Binding Candidate.Title}" FontSize="20" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding Candidate.Author}" Foreground="Gray"/>
            </StackPanel>
            <ListView Grid.Column="1" ItemsSource="{Binding Formats}" Margin="24,0,0,0">
                <ListView.View>
                    <GridView AllowsColumnReorder="False">
                        <GridViewColumn Header="格式" DisplayMemberBinding="{Binding Id}" Width="120"/>
                        <GridViewColumn Header="分辨率" DisplayMemberBinding="{Binding Resolution}" Width="120"/>
                        <GridViewColumn Header="扩展名" DisplayMemberBinding="{Binding Extension}" Width="80"/>
                        <GridViewColumn Header="大小" Width="140">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Filesize, Converter={StaticResource FileSizeConverter}}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
                <ListView.SelectedItem>
                    <Binding Path="SelectedFormat" Mode="TwoWay"/>
                </ListView.SelectedItem>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
